// Simulated DB lookup or creation
// export const findOrCreateUser = async (googleProfile) => {
//   // Normally, you’d look up the user in your database here
//   // and create them if they don’t exist.
//   const user = {
//     id: googleProfile.id,
//     name: googleProfile.displayName,
//     email: googleProfile.emails?.[0]?.value || null,
//     photo: googleProfile.photos?.[0]?.value || null
//   };

//   return user;
// };
